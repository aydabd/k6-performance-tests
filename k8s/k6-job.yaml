apiVersion: batch/v1
kind: Job
metadata:
  name: simple-k6-test
  namespace: k6-tests
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: k6
          image: ${TEST_IMAGE}
          env:
            - name: K6_INFLUXDB_ADDR
              value: "http://influxdb:8086"
          volumeMounts:
            - name: logs
              mountPath: /performance-k6/test-logs
      volumes:
        - name: logs
          emptyDir: {}
  backoffLimit: 0
